遍历该步骤中 `browser:` 下的每个操作，按照以下规则执行：

导航检查：
1. 在 `goto` 之前，确认当前页面是否已经在目标站点，如果是则跳过不必要的跳转。
2. 如果 `goto` 跳转后页面加载异常（超时、白屏、404），等待 2 秒后重试一次。
3. 每次操作后等待网络空闲（wait_for_network_idle）。
4. 如果 click/fill 的目标元素不可见，先滚动到该元素位置再操作。

会话保持：
- 如果页面导航导致登录态丢失（跳转到登录页），自动检测并走登录恢复流程。
- 登录恢复后继续执行 pipeline 后续步骤，不需要人工介入。
